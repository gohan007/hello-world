#include <stdio.h> 
#include<conio.h>
int main()
{
	int i,n A[1000];
	for(i=0;i<1000;i++)
	{ if(i==0)
	    scanf("%d ",&A[i]); 
	    else 
	    scanf(" %d",&A[i]);
	}
	for(j=0;j<1000;j++)
	{
	    printf("%d ",A[j]);
	}
	getch();
	return 0;
}
